{
  // Server authentication info
  "servers": [
    {
      "host": "162.243.202.130",
      "username": "root",
      "pem": "key.pem"
    }
  ],


  "ssl": {
    "certificate": "./ssl/bundle.crt", // this is a bundle of certificates
    "key": "./ssl/private.key", // this is the private key of the certificate
    "port": 443 // 443 is the default value and it's the standard HTTPS port
  },

  // Install MongoDB in the server, does not destroy local MongoDB on future setup
  "setupMongo": false,

  // Application name (No spaces)
  "appName": "herochef",

  // Location of app (local directory)
  "app": "..",

  // Configure environment
  "env": {
    "ROOT_URL": "https://app.hospohero.com/",
    "MONGO_URL": "mongodb://cloudninja:Bertaroo1@c144.capital.3.mongolayer.com:10144,c144.capital.2.mongolayer.com:10144/herochef",
    "MAIL_URL": "smtp://notifications%40hospohero.com:Bertaroo724@smtp.mailgun.org:587/"
  },

  // Meteor Up checks if the app comes online just after the deployment
  // before mup checks that, it will wait for no. of seconds configured below
  "deployCheckWaitTime": 15,

  // show a progress bar while uploading.
  // Make it false when you deploy using a CI box.
  "enableUploadProgressBar": true
}
